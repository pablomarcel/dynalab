"""simulator.ui.actions

(Placeholder) Centralized action creation.

The MVP currently creates actions directly in MainWindow for speed.

This module exists so we can later:
- define actions once (with shortcuts/icons)
- share between menu + toolbar + context menus
- enable/disable actions based on selection/state
"""

from __future__ import annotations
